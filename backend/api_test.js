var http = require('http'); var data = JSON.stringify({username:'chef',password:'123456'}); var req = http.request({hostname:'127.0.0.1',port:3001,path:'/api/auth/login',method:'POST',headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(data)}}, function(res){ var body=''; res.on('data',function(c){body+=c}); res.on('end',function(){ var r=JSON.parse(body); if(r.token){console.log('LOGIN OK:',r.user.nickname);}else{console.log('LOGIN FAIL:',body)}; process.exit(0); }); }); req.on('error',function(e){console.log('ERR:',e.message);process.exit(1)}); req.write(data); req.end();
